import base64
import os

# The system automatically saves uploaded images to a specific directory or we can request it.
# Actually, the image provides by the user might not be in that path. Let's find it.
